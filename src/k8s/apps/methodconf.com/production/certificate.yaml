apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: methodconf-tls
spec:
  secretName: methodconf-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
    - methodconf.com
    - www.methodconf.com
    - cms.methodconf.com
