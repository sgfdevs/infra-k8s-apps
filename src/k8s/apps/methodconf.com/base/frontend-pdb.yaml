apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: methodconf-frontend
spec:
  minAvailable: 1
  unhealthyPodEvictionPolicy: AlwaysAllow
  selector:
    matchLabels:
      app.kubernetes.io/name: methodconf-frontend
