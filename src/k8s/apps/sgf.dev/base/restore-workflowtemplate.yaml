apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: sgf-dev-restore
spec:
  entrypoint: restore
  onExit: cleanup
  serviceAccountName: sgf-dev-restore-workflow
  synchronization:
    mutexes:
      - name: sgf-dev-maintenance
  arguments:
    parameters:
      - name: snapshot-id
        value: latest
      - name: confirmation
        description: >-
          Enter "RESTORE sgf-dev-staging" or "RESTORE sgf-dev-production",
          matching the namespace where this workflow is submitted.
  activeDeadlineSeconds: 1200
  ttlStrategy:
    secondsAfterCompletion: 86400
  podGC:
    strategy: OnWorkflowCompletion
    deleteDelayDuration: 10m
  templates:
    - name: restore
      steps:
        - - name: prepare
            template: prepare
        - - name: restore-database
            template: restore-database
    - name: prepare
      container:
        image: alpine:3.22.1
        command: [/scripts/prepare-restore.sh]
        env:
          - name: TARGET_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: SNAPSHOT_ID
            value: "{{workflow.parameters.snapshot-id}}"
          - name: CONFIRMATION
            value: "{{workflow.parameters.confirmation}}"
        resources:
          requests:
            cpu: 25m
            memory: 32Mi
          limits:
            cpu: 250m
            memory: 128Mi
        volumeMounts:
          - name: scripts
            mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: sgf-dev-workflow-scripts
            defaultMode: 0555
    - name: restore-database
      container:
        image: alpine:3.22.1
        command: [/scripts/restore.sh]
        env:
          - name: TARGET_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
          - name: SNAPSHOT_ID
            value: "{{workflow.parameters.snapshot-id}}"
          - name: CONFIRMATION
            value: "{{workflow.parameters.confirmation}}"
          - name: RESTIC_REPOSITORY
            value: s3:https://s3.us-east-005.backblazeb2.com/sgfdevs-on-prem-k3s-backups/$(TARGET_NAMESPACE)
          - name: RESTIC_PASSWORD
            valueFrom:
              secretKeyRef:
                name: sgf-dev-k8up-repo-password
                key: password
          - name: AWS_ACCESS_KEY_ID
            valueFrom:
              secretKeyRef:
                name: k8up-b2-credentials
                key: account-id
          - name: AWS_SECRET_ACCESS_KEY
            valueFrom:
              secretKeyRef:
                name: k8up-b2-credentials
                key: account-key
        resources:
          requests:
            cpu: 25m
            memory: 64Mi
          limits:
            cpu: 500m
            memory: 256Mi
        volumeMounts:
          - name: scripts
            mountPath: /scripts
          - name: data
            mountPath: /data
      volumes:
        - name: scripts
          configMap:
            name: sgf-dev-workflow-scripts
            defaultMode: 0555
        - name: data
          persistentVolumeClaim:
            claimName: sgf-dev-data
    - name: cleanup
      container:
        image: alpine:3.22.1
        command: [/scripts/cleanup-restore.sh]
        env:
          - name: TARGET_NAMESPACE
            valueFrom:
              fieldRef:
                fieldPath: metadata.namespace
        resources:
          requests:
            cpu: 25m
            memory: 32Mi
          limits:
            cpu: 250m
            memory: 128Mi
        volumeMounts:
          - name: scripts
            mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: sgf-dev-workflow-scripts
            defaultMode: 0555
