apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: sgf-dev-staging-restore
spec:
  entrypoint: restore
  serviceAccountName: sgf-dev-staging-restore
  synchronization:
    mutexes:
      - name: sgf-dev-staging-maintenance
  arguments:
    parameters:
      - name: snapshot-id
      - name: confirmation
  ttlStrategy:
    secondsAfterCompletion: 86400
  podGC:
    strategy: OnWorkflowCompletion
    deleteDelayDuration: 10m
  templates:
    - name: restore
      container:
        image: registry.k8s.io/kubectl:v1.36.2
        command: [/scripts/restore.sh]
        env:
          - name: TARGET_NAMESPACE
            value: sgf-dev-staging
          - name: BACKEND_PATH
            value: sgf-dev-staging
          - name: BACKUP_PATH
            value: /sgf-dev-staging-backup.sqlite.db
          - name: WORKFLOW_UID
            value: "{{workflow.uid}}"
          - name: SNAPSHOT_ID
            value: "{{workflow.parameters.snapshot-id}}"
          - name: CONFIRMATION
            value: "{{workflow.parameters.confirmation}}"
        volumeMounts:
          - name: scripts
            mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: sgf-dev-workflow-scripts
            defaultMode: 0555
