apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gooddads-enrollment-bot-tls
  annotations:
    argocd.argoproj.io/sync-wave: "4"
spec:
  secretName: gooddads-enrollment-bot-tls
  duration: 8760h
  renewBefore: 720h
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  usages:
    - digital signature
    - key encipherment
  dnsNames:
    - gooddads-enrollment-bot.invalid
