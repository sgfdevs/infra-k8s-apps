apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hack4goodsgf-apex
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: hack4goodsgf-com-production-hack4goodsgf-www-redirect@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - hack4goodsgf.com
      secretName: hack4goodsgf-tls
  rules:
    - host: hack4goodsgf.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hack4goodsgf
                port:
                  number: 80
