apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-ui
  namespace: longhorn-system
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.middlewares: longhorn-system-longhorn-https-redirect@kubernetescrd,oauth2-proxy-oauth2-sign-in-errors@kubernetescrd,oauth2-proxy-longhorn-forward-auth@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - longhorn.sgf.dev
      secretName: longhorn-tls
  rules:
    - host: longhorn.sgf.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: longhorn-frontend
                port:
                  number: 80
