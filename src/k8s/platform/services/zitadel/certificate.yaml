apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: zitadel-tls
spec:
  secretName: zitadel-tls
  duration: 8760h
  renewBefore: 720h
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
    group: cert-manager.io
  usages:
    - digital signature
    - key encipherment
  dnsNames:
    - id.sgf.dev
