apiVersion: argoproj.io/v1alpha1
kind: WorkflowTemplate
metadata:
  name: zitadel-restore-resume
spec:
  entrypoint: resume
  serviceAccountName: zitadel-restore-workflow
  synchronization:
    mutexes:
      - name: zitadel-maintenance
  arguments:
    parameters:
      - name: confirmation
        description: Enter "RESUME zitadel" to restore replicas and Argo CD reconciliation.
  activeDeadlineSeconds: 600
  ttlStrategy:
    secondsAfterCompletion: 86400
  podGC:
    strategy: OnWorkflowCompletion
    deleteDelayDuration: 10m
  templates:
    - name: resume
      container:
        image: alpine/k8s:1.36.2
        command: [sh, -ec]
        args:
          - |
            if [ "{{workflow.parameters.confirmation}}" != "RESUME zitadel" ]; then
              echo "Confirmation must exactly match: RESUME zitadel" >&2
              exit 1
            fi
            /scripts/cleanup-restore.sh
        resources:
          requests:
            cpu: 25m
            memory: 32Mi
          limits:
            cpu: 250m
            memory: 128Mi
        volumeMounts:
          - name: scripts
            mountPath: /scripts
      volumes:
        - name: scripts
          configMap:
            name: zitadel-workflow-scripts
            defaultMode: 0555
